# 📸 Media Organizer (Сортировщик Фото и Видео)

**Media Organizer** — это мощный и умный инструмент для автоматической сортировки ваших фотографий и видео. Скрипт анализирует даты создания файлов (из метаданных EXIF, заголовков видео или имен файлов) и раскладывает их по красивой и логичной структуре папок.

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![License](https://img.shields.io/badge/License-MIT-green)

---

## ✨ Возможности

- 📂 **Структурирование по датам**: Раскладывает файлы в структуру `Год/Год.Месяц/Год.Месяц.День` (например, `2023/2023.08/2023.08.25`).
- 🧠 **Умная сортировка (Smart Sort)**: Если в месяце есть файлы только за один день, папка дня не создается, а файлы кладутся сразу в папку месяца (например, `2023/2023.08/фото.jpg`).
- ⏭️ **Умный пропуск (Smart Skip)**:
    - Пропускает уже организованные папки, чтобы не обрабатывать их повторно.
    - Сохраняет папки с пользовательскими названиями (например, `2023.08.13 День Рождения`), не разбивая их.
- 🛡️ **Защита от дубликатов**: Не перезаписывает файлы с одинаковыми именами, а добавляет к ним индекс.
- 🚫 **Предотвращение вложенности**: Умная логика не создает лишние папки (например, `2023/2023/2023.08`), если вы запускаете сортировку внутри уже существующей структуры.
- 📅 **Мощное извлечение дат**:
    - Читает **EXIF** из фотографий.
    - Читает метаданные **видео** (QuickTime, MP4 и др.).
    - Распознает даты в **именах файлов** (например, `IMG_20230825_120000.jpg`, `WhatsApp Image 2023-08-25...`).
- 🎥 **Сжатие видео**:
    - Автоматическое сжатие видео файлов (mp4, mov, etc) с использованием `ffmpeg`.
    - Сохранение оригинальных метаданных (даты съемки) с помощью `exiftool`.
    - Создание копий с суффиксом `-small` рядом с оригиналом.
- 📊 **Красивый прогресс-бар**: Наглядное отображение процесса с именем текущего файла.

## 🚀 Установка и Запуск

Самый простой способ начать работу — использовать скрипт автозапуска:

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/yourusername/media-organizer.git
   cd media-organizer
   ```

2. **Запустите скрипт**:
   ```bash
   ./run.sh
   ```
   Скрипт сам:
   - Создаст виртуальное окружение
   - Установит Python-зависимости
   - Проверит и установит системные утилиты (`ffmpeg`, `exiftool`) через Homebrew (на macOS)
   - Запустит программу

### Ручная установка (если не используете run.sh)

1. **Создайте виртуальное окружение**:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

   > **Примечание**: Для работы функции сжатия видео требуются установленные `ffmpeg` и `exiftool`.
   > На macOS их можно установить через brew: `brew install ffmpeg exiftool`

## 📖 Использование

После запуска скрипта выберите режим работы в главном меню:

1. **Сортировка фото и видео** — классический режим сортировки по папкам.
2. **Сжатие видео** — новый режим для пакетного сжатия видеофайлов.

### Режим Сортировки
Скрипт интерактивно запросит у вас:
1. **Путь к папке-источнику**: Откуда брать файлы.
2. **Путь к папке-назначению**: Куда складывать отсортированные файлы.
3. **Режим работы**:
    - `Копирование`: Файлы копируются (безопасно, исходники остаются).
    - `Перемещение`: Файлы перемещаются (экономит место).
4. **Обработка файлов без даты**: Сохранить их в отдельную папку или пропустить.

### Режим Сжатия Видео
1. Выберите пункт **2** в главном меню.
2. Укажите путь к папке с видео.
3. Выберите, нужно ли автоматически удалять файлы после сжатия:
   - **Да** — если сжатый файл меньше оригинала, удаляется оригинал; иначе удаляется сжатый файл
   - **Нет** — оба файла сохраняются
4. Скрипт найдет все видео в этой папке (и подпапках) и создаст сжатые копии `имя-small.mp4`.

> **Примечание**: Скрипт автоматически пропускает файлы, которые уже заканчиваются на `-small.mp4`, чтобы избежать повторной обработки.

### Пример структуры

**Было:**
```
Downloads/
  IMG_20230825.jpg
  DCIM/
    DSC001.ARW (дата съемки 12.01.2024)
  Video_2023-08-25.mov
```

**Стало:**
```
Sorted_Photos/
  2023/
    2023.08/
      2023.08.25/
        IMG_20230825.jpg
        Video_2023-08-25.mov
  2024/
    2024.01/
      2024.01.12/
        DSC001.ARW
```

## 🛠 Поддерживаемые форматы

- **Фото**: JPG, JPEG, PNG, HEIC, RAW (CR2, NEF, ARW, DNG), TIFF, BMP, WEBP, GIF.
- **Видео**: MP4, MOV, AVI, MKV, WMV, M4V, 3GP, MPG.

## ⚙️ Технические детали

- **Язык**: Python 3.8+
- **Библиотеки**: `Pillow` (EXIF), `pymediainfo` (видео), `tqdm` (прогресс), `colorama` (цвета).
- **Логирование**: Подробные логи сохраняются в папку `logs/`.

## 🤝 Вклад в проект

Если вы нашли ошибку или хотите предложить улучшение, пожалуйста, создайте Issue или Pull Request.

## 📄 Лицензия

Этот проект распространяется под лицензией **Creative Commons Attribution-NonCommercial 4.0 International (CC BY-NC 4.0)**.

**Вы можете:**
- ✅ Свободно использовать, копировать и модифицировать код
- ✅ Делиться проектом с другими
- ✅ Использовать в личных и образовательных целях

**Запрещено:**
- ❌ Коммерческое использование без разрешения автора

Подробности в файле [LICENSE](LICENSE).
